{{#each @stars as |star|}}
  <a href="#" {{on "click" (fn this.star.rating "setRating") }} data-test-rr={{concat "star-rating-" star.rating}}>
    {{#if @star.isFull}}
      {{fa-icon 'star'}}
    {{else}}
      {{fa-icon 'star-o'}}
    {{/if}}
  </a>
{{/each}}